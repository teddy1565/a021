function split(str,c){
    
}
function main(){
    let keyin_string;//string length = 600
    while(req.body.keyin!=undefined){//fgets(keyin_string,600,stdin)!=NULL
        keyin_string = req.body.keyin;
        char_a = ' '
        let split_str = split(keyin_string,char_a);
    }
}